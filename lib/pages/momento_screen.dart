import 'package:flutter/material.dart';

class MomentoScreen extends StatelessWidget {
  const MomentoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          Text(
            'Momento screen',
          ),
        ],
      ),
    );
  }
}
